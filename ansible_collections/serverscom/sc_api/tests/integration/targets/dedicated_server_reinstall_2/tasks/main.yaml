---
# Reinstall tests for server2: short-timeout wait test.
- name: Check if there are sc_token and sc_endpoint variables
  no_log: true
  fail:
    msg: 'You need to define sc_token and sc_endpoint variables in tests/integration/integration_config.yml'
  when: not sc_endpoint or not sc_token

- name: Test1, reinstall with wait-mode with very short wait time
  sc_dedicated_server_reinstall:
    id: '{{ existing_server2_id }}'
    drives_layout_template: raid1-simple
    operating_system_id: "{{ dedicated_test_reinstall_os_id }}"
    hostname: reinstall2
    wait: 10
    update_interval: 5
  environment:
    SERVERSCOM_API_TOKEN: '{{ sc_token }}'
    SERVERSCOM_API_URL: '{{ sc_endpoint }}'
  register: test1
  failed_when: false

- name: Check Test1
  assert:
    that:
      - test1.timeout > 10
      - "'Server is not ready' in test1.msg"
