---
name: Test and build

"on":
  push:
    paths-ignore:
      - ".github/**"
      - "*.md"
  workflow_dispatch:

jobs:
  lint:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Install yamllint
        run: sudo apt install yamllint
      - name: Lint
        run: yamllint .

  unit_and_sanity:
    runs-on: ubuntu-24.04
    strategy:
      fail-fast: false
      matrix:
        include:
          - ansible_core: "2.18"
            python_version: "3.13"
          - ansible_core: "2.19"
            python_version: "3.13"
          - ansible_core: "2.19"
            python_version: "3.13"
          - ansible_core: "2.20"
            python_version: "3.13"
          - ansible_core: "2.20"
            python_version: "3.14"
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Python ${{ matrix.python_version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python_version }}
      - name: Install ansible ${{ matrix.ansible_core }}
        run: |
          python -m pip install --upgrade pip
          pip install 'ansible-core~=${{ matrix.ansible_core }}.0' requests pytest
      - name: Sanity tests
        run: ansible-test sanity --requirements --python ${{ matrix.python_version }}
        working-directory: ansible_collections/serverscom/sc_api
      - name: Unit tests
        run: ansible-test units --requirements --python ${{ matrix.python_version }}
        working-directory: ansible_collections/serverscom/sc_api
      - name: Token-free integration tests
        run: ansible-test integration --requirements --python ${{ matrix.python_version }} sc_no_token_tests
        working-directory: ansible_collections/serverscom/sc_api
      - name: Try-build by ansible-galaxy
        run: ansible-galaxy collection build
        working-directory: ansible_collections/serverscom/sc_api

  integration_tests_cloud:
    runs-on: ubuntu-24.04
    needs:
      - unit_and_sanity
      - lint
    concurrency:
      group: cloud
      cancel-in-progress: false
    steps:
      - name: Report used token
        run: echo ${{ secrets.SC_TOKEN }} | tail -c 5
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      - name: Configure integration tests
        run: |
          envsubst < integration_config.yml.template > ansible_collections/serverscom/sc_api/tests/integration/integration_config.yml
        env:
          SC_TOKEN: "${{ secrets.SC_TOKEN }}"
      - name: Integration tests
        run: ansible-test integration --requirements --python 3.13
          ssh_key
          cloud_computing_flavors_info
          cloud_computing_regions_info
          cloud_computing_images_info
          cloud_computing_openstack_credentials
          cloud_computing_instance
          cloud_computing_instance_info
          cloud_computing_instance_ptr
          cloud_computing_instances_info
          cloud_computing_instance_state
        # cloud_computing_instance_reinstall  - broken and unfinished

        working-directory: ansible_collections/serverscom/sc_api
      - name: Cleanup secrets
        if: always()
        run: |
          dd if=/dev/zero bs=4k count=4 of=ansible_collections/serverscom/sc_api/tests/integration/integration_config.yml
          sync
          sync

  integration_tests_bm:
    runs-on: ubuntu-24.04
    needs:
      - unit_and_sanity
      - lint
    concurrency:
      group: BM
      cancel-in-progress: false
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      - name: Configure integration tests
        run: |
          envsubst < integration_config.yml.template > ansible_collections/serverscom/sc_api/tests/integration/integration_config.yml
        env:
          SC_TOKEN: "${{ secrets.SC_TOKEN }}"
      - name: Integration tests
        run: ansible-test integration --requirements --python 3.13
          ssh_key
          baremetal_locations_info
          baremetal_servers_info
          baremetal_os_list
          dedicated_server_info
          dedicated_server_reinstall_quick
          dedicated_server_reinstall_long
          dedicated_server_power

        working-directory: ansible_collections/serverscom/sc_api
      - name: Cleanup secrets
        if: always()
        run: |
          dd if=/dev/zero bs=4k count=4 of=ansible_collections/serverscom/sc_api/tests/integration/integration_config.yml
          sync
          sync

  integration_tests_l2:
    runs-on: ubuntu-24.04
    needs: integration_tests_bm
    concurrency:
      group: BM
      cancel-in-progress: false
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      - name: Configure integration tests
        run: |
          envsubst < integration_config.yml.template > ansible_collections/serverscom/sc_api/tests/integration/integration_config.yml
        env:
          SC_TOKEN: "${{ secrets.SC_TOKEN }}"
      - name: Integration tests
        run: ansible-test integration --requirements --python 3.13
          l2_segment
          l2_segment_aliases
          l2_segments_info

        working-directory: ansible_collections/serverscom/sc_api
      - name: Cleanup secrets
        if: always()
        run: |
          dd if=/dev/zero bs=4k count=4 of=ansible_collections/serverscom/sc_api/tests/integration/integration_config.yml
          sync
          sync

  integration_tests_lb_instances:
    runs-on: ubuntu-24.04
    needs:
      - unit_and_sanity
      - lint
    concurrency:
      group: LB
      cancel-in-progress: false
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      - name: Configure integration tests
        run: |
          envsubst < integration_config.yml.template > ansible_collections/serverscom/sc_api/tests/integration/integration_config.yml
        env:
          SC_TOKEN: "${{ secrets.SC_TOKEN }}"
      - name: Integration tests
        run: ansible-test integration --requirements --python 3.13
          load_balancer_instance_info
          load_balancer_instance_l4
          load_balancer_instance_l7
          load_balancer_instances_list

        working-directory: ansible_collections/serverscom/sc_api
      - name: Cleanup secrets
        if: always()
        run: |
          dd if=/dev/zero bs=4k count=4 of=ansible_collections/serverscom/sc_api/tests/integration/integration_config.yml
          sync
          sync

  integration_tests_rbs_volumes:
    runs-on: ubuntu-24.04
    needs:
      - unit_and_sanity
      - lint
    concurrency:
      group: RBS
      cancel-in-progress: false
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      - name: Configure integration tests
        run: |
          envsubst < integration_config.yml.template > ansible_collections/serverscom/sc_api/tests/integration/integration_config.yml
        env:
          SC_TOKEN: "${{ secrets.SC_TOKEN }}"
      - name: Integration tests
        run: ansible-test integration --requirements --python 3.13
          rbs_flavors_info
          rbs_volume
          rbs_volume_info
          rbs_volume_credentials_reset

        working-directory: ansible_collections/serverscom/sc_api
      - name: Cleanup secrets
        if: always()
        run: |
          dd if=/dev/zero bs=4k count=4 of=ansible_collections/serverscom/sc_api/tests/integration/integration_config.yml
          sync
          sync

  integration_tests_sbm:
    runs-on: ubuntu-24.04
    needs:
      - unit_and_sanity
      - lint
    concurrency:
      group: SBM
      cancel-in-progress: false
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      - name: Configure integration tests
        run: |
          envsubst < integration_config.yml.template > ansible_collections/serverscom/sc_api/tests/integration/integration_config.yml
        env:
          SC_TOKEN: "${{ secrets.SC_TOKEN }}"
      - name: Integration tests
        run: ansible-test integration --requirements --python 3.13
          sbm_flavor_models_info
          sbm_server_lifecycle
          sbm_servers_info
          sbm_os_list

        working-directory: ansible_collections/serverscom/sc_api
      - name: Cleanup secrets
        if: always()
        run: |
          dd if=/dev/zero bs=4k count=4 of=ansible_collections/serverscom/sc_api/tests/integration/integration_config.yml
          sync
          sync

  build:
    runs-on: ubuntu-24.04
    needs:
      - integration_tests_bm
      - integration_tests_cloud
      - integration_tests_l2
      - integration_tests_lb_instances
      - integration_tests_rbs_volumes
      - integration_tests_sbm
    outputs:
      version: ${{ steps.version.outputs.version }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      - name: Remove tests from final distribution
        run: rm -r tests/
        working-directory: ansible_collections/serverscom/sc_api
      - name: Build collection
        run: ansible-galaxy collection build
        working-directory: ansible_collections/serverscom/sc_api
      - name: Extract version
        id: version
        run: |
          version=$(grep ^version galaxy.yml|awk '{print $2}')
          echo "version=$version" >> "$GITHUB_OUTPUT"
        working-directory: ansible_collections/serverscom/sc_api
      - name: Print artifact info
        run: |
          ls -la serverscom-sc_api-${{ steps.version.outputs.version }}.tar.gz
          sha512sum serverscom-sc_api-${{ steps.version.outputs.version }}.tar.gz
          tar tzf serverscom-sc_api-${{ steps.version.outputs.version }}.tar.gz
        working-directory: ansible_collections/serverscom/sc_api
      - name: Save artifact
        if: ${{ endsWith(github.ref, steps.version.outputs.version) }}
        uses: actions/upload-artifact@v4
        with:
          name: sc_api
          path: ansible_collections/serverscom/sc_api/serverscom-sc_api-${{ steps.version.outputs.version }}.tar.gz

  publish:
    runs-on: ubuntu-24.04
    needs: build
    if: ${{ endsWith(github.ref, needs.build.outputs.version) }}
    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: sc_api
      - name: Debug output
        run: ls -la serverscom-sc_api-${{needs.build.outputs.version }}.tar.gz
      - name: Upload collection to galaxy
        run: ansible-galaxy collection publish serverscom-sc_api-${{ needs.build.outputs.version }}.tar.gz --api-key="${{ secrets.GALAXY_API_TOKEN }}"
